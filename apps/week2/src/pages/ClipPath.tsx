import ClipPathTransition from "../@layout/ClipPathTransition";
import Layout from "../@layout/layout";

function ClipPath() {
  return (
    <ClipPathTransition>
      <Layout>ClipPath Animation</Layout>
    </ClipPathTransition>
  );
}

export default ClipPath;
