import Layout from "../@layout/layout";
import BackgroundTransition from "../@layout/BackgroundTransition";

function Background() {
  return (
    <Layout>
      <BackgroundTransition>background Animation</BackgroundTransition>
    </Layout>
  );
}

export default Background;
